---
summary: "Add hosted server endpoint, FUNDING.yml, publish-mcp script, hosted URL badge"
breaking: false
security: false
---

# 0.1.5 — 2026-05-26

## Added

- **Hosted server endpoint:** `https://reference-data.caseyjhand.com/mcp` listed in README
- **`.github/FUNDING.yml`:** GitHub Sponsors and Buy Me a Coffee links
- **`publish-mcp` script:** MCP Registry publish via `mcp-publisher` (aligned to fleet standard)
- **`ai-agent` keyword** in `package.json` for npm discoverability

## Changed

- **`package.json` `files`:** removed `changelog/` and `AGENTS.md` (not shipped in npm package)
- **README Bun badge:** `v1.3.0` → `v1.3.11` to match `packageManager` field
- **`manifest.json` version** synced: `0.1.3` → `0.1.5` (was lagging two releases behind)

107 tests pass; `bun run devcheck` clean.
