---
summary: "Scope npm package to @cyanheads/reference-data-mcp-server; fix bundle script and author field"
breaking: false
security: false
---

# 0.1.2 — 2026-05-24

## Fixed

- **`package.json` `name`**: corrected from bare `reference-data-mcp-server` to scoped `@cyanheads/reference-data-mcp-server`.
- **`package.json` `bundle` script**: replaced `npm run build` with `bun run build` to match project runtime conventions.
- **`package.json` `author`**: aligned to `cyanheads <casey@caseyjhand.com> (https://github.com/cyanheads/reference-data-mcp-server#readme)` format used across the @cyanheads ecosystem.
- **`server.json` `packages[].identifier`**: both entries updated from unscoped to `@cyanheads/reference-data-mcp-server`.
- **`README.md`**: `<h1>` title, npm badge URL, Cursor install badge (base64 config), and VS Code install badge (URL-encoded config) all updated to reference the scoped package name.
